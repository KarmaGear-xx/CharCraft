# CharCraft — SillyTavern 角色卡工具(桌面版)

一个类 Ginger 的 **SillyTavern 角色卡开发/编辑工具**,以 **Windows 便携版单文件 `.exe`** 交付,免安装、双击即用。

> 本版为 Electron 桌面版,完全重写,不依赖系统浏览器,从根本上规避了网页版在 `file://` 下的白屏问题。

---

## 一、直接使用(成品)

1. 双击 **`release/CharCraft.exe`**(约 95 MB,单文件,可随意拷贝移动)。
2. 首次运行 Windows SmartScreen 可能提示「未知发布者」→ 点「更多信息」→「仍要运行」即可(本工具未做代码签名)。
3. 点顶部「导入」选择一张 `.png` / `.json` 角色卡(原生文件对话框)。
4. 编辑字段、用 AI 生成、维护世界书,再点「导出 .png / 导出 .json」保存(原生另存为对话框)。
5. 关闭后重新打开,上次的草稿会自动恢复。

---

## 二、功能

- **导入**:`.png`(解析 PNG 内嵌 `chara`/`ccv3`)与 `.json`,兼容 `chara_card_v2` / `chara_card_v3`。
- **可视化表单**:核心字段 + 「高级选项」折叠区;每个字段旁 = 启用/禁用勾选框 + ✨AI 改写按钮。
- **AI 辅助生成**:
  - 「一句话设定」→ 整卡起草;每个字段独立生成/改写;世界书条目 AI 生成。
  - 由**主进程直连**大模型接口,**无浏览器 CORS 限制**,DeepSeek 直连也可用。
  - 覆盖保护(整卡生成前 4 种策略)、费用提醒(每次启动后首次生成)。
- **世界书**:手动/AI 生成条目,绑定到角色卡,随卡导出。
- **导出**:`.json` 与 `.png`(写回 `tEXt` 的 chara/ccv3 块,SillyTavern 可识别)。
- **中英双语**:界面可切换;卡片数据字段名与内容恒为英文。

---

## 三、AI 设置

顶部「AI 设置」填三项:**接口地址 / API Key / 模型名**,内置 OpenRouter、DeepSeek 两个预设,也支持自定义 OpenAI 兼容地址。

- **API Key 只存本机应用数据目录**,绝不外传,只发给你填写的服务商。
- 应用数据目录:`%APPDATA%\CharCraft\`(含 `config.json` 与 `draft.json`)。

---

## 四、从源码构建(可选)

要求:Node.js ≥ 20、pnpm。

```bash
pnpm install
pnpm typecheck          # 类型检查
pnpm test               # 单元测试(demo 卡往返 + 渲染挂载)
pnpm build              # 编译 main/preload + 渲染进程 → out/
pnpm package            # electron-builder 打包 → release/CharCraft.exe
```

---

## 五、目录结构

```
src/
  main/       # Electron 主进程(窗口、对话框、文件、存储、AI 直连)
  preload/    # contextBridge 安全桥(window.api)
  renderer/   # React 界面 + 核心逻辑(PNG/卡片/AI/状态/UI)
  shared/     # 跨进程共享类型与 IPC 契约
demo/         # 社区角色卡样例(测试用)
tests/        # vitest 测试
out/          # 构建产物(main/preload/renderer)
release/      # 打包产物(CharCraft.exe)
docs/         # 需求与实现方案
```

---

## 六、说明

- 首次打包未设置自定义图标(使用默认 Electron 图标),未做代码签名 —— 这两项可在后续里程碑补充。
- 依赖 npm/npmmirror 镜像下载 Electron 运行时;打包脚本已内置镜像与本地缓存配置。
